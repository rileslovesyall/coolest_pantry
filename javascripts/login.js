$(document ).ready(function() {
    $('button').click(function (){
      console.log(params);
      // $.post('http://localhost:9393/api/v1/token', {
      //   email: params['email']
      // })
    });
});